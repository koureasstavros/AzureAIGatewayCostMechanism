OpenAI Text V1 Completions
---------------------------------------
/openai/v1/chat/completions
---------------------------------------
<rewrite-uri template="/openai/v1/chat/completions" copy-unmatched-params="false" />
<set-variable name="jsonBody" value="@{
	var body = context.Request.Body.As<JObject>(preserveContent: true);
	body["temperature"] = 1;   // <-- overwrite here
	return body.ToString();
}" />
<set-body>@((string)context.Variables["jsonBody"])</set-body>
---------------------------------------
<policies>
    <inbound>
        <base />
        <rewrite-uri template="/openai/v1/chat/completions" copy-unmatched-params="false" />
		<set-variable name="requestBody" value="@{
            var body = context.Request.Body.As<JObject>(preserveContent: true);
            bool isStreamingRequest = body["stream"]?.Value<bool>() ?? false;
            if (isStreamingRequest)
            {
                body["stream_options"] = new JObject
                {
                    ["include_usage"] = true // <-- overwrite here
                };
            }
            return body.ToString();
        }" />
        <!-- Replace request body -->
        <set-body>@((string)context.Variables["requestBody"])</set-body>
        <set-variable name="modelId" value="@{
            string raw = (string)context.Variables["requestBody"];
            if (string.IsNullOrWhiteSpace(raw))
            { return null; }
            try
            {
                var body = JObject.Parse(raw);
                return body?["model"]?.Value<string>();
            }
            catch
            { return null; }
        }" />
        <!-- Calculate current year-month -->
        <set-variable name="yearMonth" value="@{
            return DateTime.UtcNow.ToString("yyyy-MM");
        }" />
        <!-- Load current accumulated cost from cache -->
        <cache-lookup-value key="@($"quota/{context.Subscription.Key}/cost/{context.Variables["yearMonth"]}")" variable-name="storedCost" />
        <!-- Block if already over limit before even hitting the backend -->
        <choose>
            <when condition="@(Convert.ToDouble(context.Variables.GetValueOrDefault("storedCost", (object)0)) > 100)">
                <return-response>
                    <set-status code="429" reason="Cost Limit Exceeded" />
                    <set-body>Cost quota exceeded</set-body>
                </return-response>
            </when>
        </choose>
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
		<!-- Calculate current year-month -->
        <set-variable name="yearMonth" value="@{
            return DateTime.UtcNow.ToString("yyyy-MM");
        }" />
        <!-- 1. Read body ONCE into a variable -->
        <set-variable name="responseBody" value="@(context.Response.Body.As<string>(preserveContent: true))" />
        <set-variable name="isStreamingResponse" value="@{
            string raw = (string)context.Variables["responseBody"];
            if (string.IsNullOrWhiteSpace(raw)) 
            { return false; }
            string trimmed = raw.TrimStart();
            return trimmed.Length == 0 || trimmed[0] != '{';
        }" />
		<set-variable name="responseJson" value="@{
            bool isStreaming = (bool)context.Variables["isStreamingResponse"];
            string raw = (string)context.Variables["responseBody"];

            if (!isStreaming)
            {
                // --- Non-streaming path: body IS the response object directly ---
                try { return JObject.Parse(raw); }
                catch { return new JObject(); }
            }

            if (string.IsNullOrWhiteSpace(raw))
            { return new JObject(); }

            var lines = raw.Split('\n');

            string id = null;
            string model = null;
            long created = 0;
            string finishReason = null;
            string role = "assistant";
            var contentBuilder = new System.Text.StringBuilder();
            JObject usage = null;
            JObject responsesApiCompleted = null; // in case backend used Responses API shape

            foreach (var rawLine in lines)
            {
                var line = rawLine.TrimEnd('\r').Trim();
                if (!line.StartsWith("data: "))
                { continue; }

                var payload = line.Substring(6).Trim();
                if (payload == "[DONE]" || string.IsNullOrWhiteSpace(payload))
                { continue; }

                JObject evt;
                try { evt = JObject.Parse(payload); }
                catch { continue; } // skip partial/obfuscated lines

                // --- Responses API streaming: one event has the full final object ---
                var eventType = (string)evt["type"];
                if (eventType == "response.completed")
                {
                    responsesApiCompleted = evt["response"] as JObject;
                    continue;
                }

                // --- Chat Completions streaming: accumulate fields across chunks ---
                if (id == null) { id = (string)evt["id"]; }
                if (model == null) { model = (string)evt["model"]; }
                if (created == 0) { created = evt["created"]?.Value<long>() ?? 0; }

                var choice = evt["choices"]?.FirstOrDefault() as JObject;
                if (choice != null)
                {
                    var delta = choice["delta"] as JObject;
                    if (delta != null)
                    {
                        var deltaRole = (string)delta["role"];
                        if (!string.IsNullOrEmpty(deltaRole)) { role = deltaRole; }

                        var deltaContent = (string)delta["content"];
                        if (!string.IsNullOrEmpty(deltaContent)) { contentBuilder.Append(deltaContent); }
                    }

                    var fr = (string)choice["finish_reason"];
                    if (!string.IsNullOrEmpty(fr)) { finishReason = fr; }
                }

                // Final chunk carries usage at top level (stream_options.include_usage = true)
                if (evt["usage"] != null && evt["usage"].Type != JTokenType.Null)
                {
                    usage = evt["usage"] as JObject;
                }
            }

            // If backend used Responses API shape, that single event already has everything
            if (responsesApiCompleted != null)
            { return responsesApiCompleted; }

            // Otherwise, build one consolidated Chat-Completions-style object
            var result = new JObject
            {
                ["id"] = id,
                ["object"] = "chat.completion",
                ["created"] = created,
                ["model"] = model,
                ["choices"] = new JArray
                {
                    new JObject
                    {
                        ["index"] = 0,
                        ["message"] = new JObject
                        {
                            ["role"] = role,
                            ["content"] = contentBuilder.ToString()
                        },
                        ["finish_reason"] = finishReason
                    }
                },
                ["usage"] = usage ?? new JObject()
            };

            return result;
        }" />
        <!-- 2. Extract token usage from the cached variable -->
        <set-variable name="actualInputTokens" value="@{
            var body = (JObject)context.Variables["responseBody"];
            var inputTokens = body?["usage"]?["prompt_tokens"]?.Value<int>() ?? 0;
            return inputTokens;
        }" />
        <set-variable name="actualCachedInputTokens" value="@{
            var body = (JObject)context.Variables["responseJson"];
            var promptDetails = body?["usage"]?["prompt_tokens_details"] as JObject;
            var cachedTokens = promptDetails?["cached_tokens"]?.Value<int>() ?? 0;
            return cachedTokens;
        }" />
        <set-variable name="actualOutputTokens" value="@{
            var body = (JObject)context.Variables["responseBody"];
            var outputTokens = body?["usage"]?["completion_tokens"]?.Value<int>() ?? 0;
            return outputTokens;
        }" />
        <!-- 3. Load existing counters from cache -->
        <cache-lookup-value key="@($"quota/{context.Subscription.Key}/cost/{context.Variables["yearMonth"]}")" variable-name="storedCost" />
        <!-- 4. Cost rates -->
        <set-variable name="inputCostPerM" value="@{
            var model = (string)context.Variables["modelId"];
            if (string.IsNullOrEmpty(model))
            { return 0.0; }
             if (model.IndexOf("GPT54NANO-GS", StringComparison.OrdinalIgnoreCase) >= 0)
            { return 0.2; }
            if (model.IndexOf("GPT54MINI-GS", StringComparison.OrdinalIgnoreCase) >= 0)
            { return 0.75; }
            if (model.IndexOf("GPT54-GS", StringComparison.OrdinalIgnoreCase) >= 0)
            { return 2.5; }
            if (model.IndexOf("GPT55-GS", StringComparison.OrdinalIgnoreCase) >= 0)
            { return 5.0; }
            return 5.0; // Default fallback
        }" />
        <set-variable name="inputCachedCostPerM" value="@{
            var model = (string)context.Variables["modelId"];
            if (string.IsNullOrEmpty(model))
            { return 0.0; }            
            if (model.IndexOf("GPT54NANO-GS", StringComparison.OrdinalIgnoreCase) >= 0)
            { return 0.02; }
            if (model.IndexOf("GPT54MINI-GS", StringComparison.OrdinalIgnoreCase) >= 0)
            { return 0.075; }
            if (model.IndexOf("GPT54-GS", StringComparison.OrdinalIgnoreCase) >= 0)
            { return 0.25; }
            if (model.IndexOf("GPT55-GS", StringComparison.OrdinalIgnoreCase) >= 0)
            { return 0.5; }
            return 1.0; // Default fallback            
        }" />
        <set-variable name="outputCostPerM" value="@{
            var model = (string)context.Variables["modelId"];
            if (string.IsNullOrEmpty(model))
            { return 0.0; }
            if (model.IndexOf("GPT54NANO-GS", StringComparison.OrdinalIgnoreCase) >= 0)
            { return 1.25; }
            if (model.IndexOf("GPT54MINI-GS", StringComparison.OrdinalIgnoreCase) >= 0)
            { return 4.5; }
            if (model.IndexOf("GPT54-GS", StringComparison.OrdinalIgnoreCase) >= 0)
            { return 15.0; }
            if (model.IndexOf("GPT55-GS", StringComparison.OrdinalIgnoreCase) >= 0)
            { return 30.0; }
            return 30.0; // Default fallback
        }" />
        <!-- 5. Calculate cost -->
        <set-variable name="inputCost" value="@{
            double rate = (double)context.Variables["inputCostPerM"];
			double tokens = (double)(int)context.Variables["actualInputTokens"];
			double cachedrate = (double)context.Variables["inputCachedCostPerM"];
			double cachedtokens = (double)(int)context.Variables["actualCachedInputTokens"];
            double cost = rate * (tokens - cachedtokens) / 1000000.0;
            double cachedcost = cachedrate * cachedtokens / 1000000.0;
			return cost + cachedcost;
        }" />
        <set-variable name="outputCost" value="@{
            double rate = (double)context.Variables["outputCostPerM"];
			double tokens = (double)(int)context.Variables["actualOutputTokens"];
            double cost = rate * tokens / 1000000.0;
			return cost;
        }" />
        <!-- 6. Calculate current cost -->
        <set-variable name="currentCost" value="@{
            double inputCost = (double)context.Variables["inputCost"];
            double outputCost = (double)context.Variables["outputCost"];
			double currentCost = inputCost + outputCost;
            return currentCost;
        }" />
        <!-- 7. Increment counters -->
        <set-variable name="newCostTotal" value="@{
            double previous = 0;
            if (context.Variables.ContainsKey("storedCost")) {
                previous = (double)context.Variables["storedCost"];
            }
            double current = (double)context.Variables["currentCost"];
            return previous + current;
        }" />
        <!-- 8. Store updated counters back into cache (30 days) -->
        <cache-store-value key="@($"quota/{context.Subscription.Key}/cost/{context.Variables["yearMonth"]}")" value="@((double)context.Variables["newCostTotal"])" duration="2592000" />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>
---------------------------------------
{"model":"<model-deployment>","messages":[{"role":"system","content":"You are a helpful assistant"},{"role":"user","content":"How are you?"}]}
---------------------------------------
{
	"id": "string",
	"object": "chat.completion",
	"created": 0,
	"model": "string",
	"choices": [
		{
			"index": 0,
			"finish_reason": {},
			"message": {
				"role": {},
				"content": "string",
				"reasoning_content": "string",
				"tool_calls": [
					{
						"id": "string",
						"type": "function",
						"function": {
							"name": "string",
							"arguments": "string"
						}
					}
				],
				"audio": {
					"id": "string",
					"expires_at": 0,
					"data": "string",
					"format": {},
					"transcript": "string"
				}
			}
		}
	],
	"usage": {
		"completion_tokens": 0,
		"prompt_tokens": 0,
		"total_tokens": 0,
		"completion_tokens_details": {
			"audio_tokens": 0,
			"reasoning_tokens": 0,
			"total_tokens": 0
		},
		"prompt_tokens_details": {
			"audio_tokens": 0,
			"cached_tokens": 0
		}
	}
